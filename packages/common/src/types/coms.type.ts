// Reserved for coms-related shared types. Kept as an explicit empty module so
// `export * from './coms.type'` in the barrel resolves under isolatedModules /
// esbuild (a zero-byte file is not a valid module).
export {};
