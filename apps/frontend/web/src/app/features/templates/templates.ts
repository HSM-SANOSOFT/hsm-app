import { Component } from '@angular/core';

/**
 * Placeholder for the template authoring surface (metadata bar + Monaco editor
 * + sample data + live preview + save gate). Replaced in U13/U14 (R12–R17).
 */
@Component({
  selector: 'app-templates',
  template: `
    <section class="feature-placeholder" data-testid="templates-placeholder">
      <h1>Templates</h1>
      <p>The template editor and live preview arrive in U13/U14.</p>
    </section>
  `,
})
export class Templates {}
